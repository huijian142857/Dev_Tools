/*
* Author:  caoshanshan
* Email:   me@dreamyouxi.com

 */
using UnityEngine;
using System.Collections;
using System.Runtime;
using System.Runtime.InteropServices;
using System.Threading;
using UnityEngine.UI;
using System;

// interface of udt for c# client
public  class UdtImpl
{
   /* [DllImport("udt")]
    public static extern int StartUp();

    [DllImport("udt")]
    public static extern bool Connected(string ip, string port);

    public static bool Connected(string ip, int port)
    {
        return Connected(ip, port.ToString());
    }
    [DllImport("udt")]
    public static extern int Recv();

    [DllImport("udt")]
    public static extern void Send(string msg, int len);

    [DllImport("udt")]
    public static extern IntPtr GetBuffer();
    [DllImport("udt")]
    public static extern int main11();*/
}
